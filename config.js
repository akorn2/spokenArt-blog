var port = 9080;

var config = {
    port: port,
    baseUrl : "http://localhost:" + port,
    pageTitle: 'Spoken Art - Bike Share - Case Studies',
    itemsPerPage: 5,
    maxPageButtons: 3
};

module.exports = config;
